$(document).ready(function(){

	// Easy slider section
	// Featured slider setting
	$("#strip").easySlider({ 
		speed: 800,
		pause: 18000,
		auto: true,
		continuous: true,
		controlsShow: false
	}); // END Featured slider setting
	
});
